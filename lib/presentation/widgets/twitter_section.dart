import 'package:flutter/material.dart';

class TwitterSectionWidget extends StatelessWidget {
  const TwitterSectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}